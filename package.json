{
  "name": "ai-roi-calculator",
  "version": "0.3.0",
  "description": "Local-first AI ROI calculator for RPE, margin, labor-cost payback, and controlled pilot scenarios.",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "start": "python3 -m http.server 8000",
    "test": "node --test *.test.js",
    "check": "node --check calculator.js && node --check locale-packs.js && node --check i18n.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
